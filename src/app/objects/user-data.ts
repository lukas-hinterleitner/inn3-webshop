export interface UserData {
    id: string;

    // general
    firstname: string;
    lastname: string;

    // address
    country: string;
    city: string;
    zip: string;
    address: string;

    // credentials
    email: string;
    pwd: string;
}
